<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BinSOS"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Additional Activities -->
        <activity android:name=".Locations" />
        <activity android:name=".LocationItem" />
        <activity android:name=".LocationAdapter" />
        <activity android:name=".FirstAidActivity" />
        <activity android:name=".LocationUtils" />
        <activity android:name=".FloodSurvivalActivity" />
        <activity android:name=".BleedingWoundsActivity" />
        <activity android:name=".BreathingAndHeartActivity" />
        <activity android:name=".BurnsActivity" />
        <activity android:name=".InjuriesAndEmergenciesActivity" />
    </application>
</manifest>
